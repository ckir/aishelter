# https://lefthook.dev

pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt --check
    clippy:
      glob: "*.rs"
      run: cargo clippy --workspace -- -D warnings

pre-push:
  commands:
    test:
      run: cargo nextest run --workspace
